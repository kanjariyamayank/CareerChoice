<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="demo.mayank.myallbooks">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContectusActivity"></activity>
        <activity android:name=".PlaceOrderActivity" />
        <activity android:name=".AppleDetailsActivity" />
        <activity android:name=".FruitsDetailsActivity" />
        <activity android:name=".SopingActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".ReagistrationActivity" />
        <activity android:name=".CategoriesActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".BookDetailActivity" />
        <activity android:name=".BooksActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>